{
  "name": "vytreos-preview",
  "version": "0.0.0",
  "private": true,
  "description": "Local preview server for Vytreos (Vercel-style static site + /api/analyze).",
  "type": "module",
  "engines": { "node": ">=18" },
  "keywords": ["retinal", "wellness", "ai-vision", "pwa", "vercel"],
  "scripts": {
    "start": "node server.js",
    "test": "node test-accuracy.mjs && node test-vq.mjs && node test-premium.mjs"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  }
}
